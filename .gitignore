# Godot 4
.godot/
.import/
export.cfg
export_credentials.cfg

# Exported builds
build/
*.pck
*.wasm
*.exe
*.apk
*.aab

# Capture output from tools/capture.gd — look at them, don't commit them
_shots/

# Q-032: the browser build's authored levels, synced by tools/sync-levels.sh
# from the deployed upstream tree. Generated — never edit, never commit.
levels/

# Editor / OS cruft
.DS_Store
Thumbs.db
*.tmp
